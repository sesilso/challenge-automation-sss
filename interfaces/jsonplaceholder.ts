export interface postsResponseData{
    id: number;
    title: string;
    body: string;
    userId: number;
}

export interface postTestData{
    userId: number;
    title: string;
    body: string;
}